# .github/workflows/dependabot-automerge.yml
# Disabled - dependabot PR builds were triggering unnecessary Vercel deployments
# Henderson Standard: preview-only by default, Roy confirms before production
# Updated: 2026-03-14

name: Dependabot Auto-merge (Disabled)

on:
  workflow_dispatch:

jobs:
  # Intentionally empty - workflow disabled to prevent auto-deploys from dependabot PRs
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependabot automerge disabled per Henderson Standard"
